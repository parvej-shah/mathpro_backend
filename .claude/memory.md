# Memory — Append-Only Execution Log

Continuity across sessions. **Append only.** Never rewrite or prune past entries
(`invariants.md` § Documentation). One entry per meaningful change.

Format: `YYYY-MM-DD — <what changed> | scope: <files/feature> | why: <reason>`

---

2026-06-16 — (1) Added migration 016_progress_indexes.sql: idx_progress_user on progress(user_id) and idx_progress_module on progress(module_id) to fix full-table-scan on leaderboard/dashboard queries. (2) Fixed completeModule in service/managerial/module.js: non-QUIZ completion now returns early (success) if a progress row already exists, instead of DELETE+INSERT which was silently overwriting quiz scores. (3) Fixed recordView in service/user/moduleViews.js: after INSERT, evicts rows beyond the 5 most recent per (user_id, course_id) to bound table growth. | scope: database/migrations/016_progress_indexes.sql, service/managerial/module.js, service/user/moduleViews.js | why: missing indexes caused O(n) leaderboard scans; progress overwrite regressed quiz scores; unbounded view rows would slow queries over time. Clients affected: Frontend (progress/leaderboard/dashboard reads), no contract change.

2026-06-16 — Removed email/phone duplication from profile JSONB blob. GET and PUT responses in getUserProfileForCheckout / updateUserProfileForCheckout now always source email+phone from managerial_auth top-level columns, never from profile JSONB. profile JSONB no longer writes email/phone keys on update. | scope: service/user/profile.js | why: top-level columns are canonical; writing them into JSONB created a second source of truth that drifted after updates

2026-06-14 — Established `.claude/` orchestration architecture (invariants.md, conventions.md, this log, FEATURES/{course-bundle,roles-permissions}/{purpose,rules}.md), mirroring the MathPro_Frontend repo's system. | scope: .claude/\*\*, AGENTS.md | why: prevent context decay + scope-creep refactors as the API surface grows; give agents a routing map into course/bundle and RBAC surfaces and the existing docs/swagger contract references.
2026-06-14 — Marked `bsse1610@iit.du.ac.bd` as the admin email for token generation and backend admin-context work. | scope: .claude/memory.md | why: preserve the known admin identity for future backend tasks or tests.
2026-06-14 — Added backend-managed featured-course selection APIs for the `/courses` page slider, including public read access and admin CRUD/reordering support. | scope: featured-course API surface consumed by MathPro_Frontend + mathpro_admin | why: move the courses-page hero slider from implicit list ordering to explicit admin-controlled featured-course selection.
2026-06-14 — Optimized `GET /user/payment/history` (`getPaymentHistoryAndEnrollments`): removed the per-request `information_schema.columns` checks and dead fallback query branches (coupon_id columns confirmed present since base schema), added composite indexes on `coupon_usage(user_id, course_id, transaction_id)` and `coupon_usage(user_id, bundle_id, transaction_id)` via migration `013_payment_history_indexes.sql`, and documented the endpoint in `swagger/user.yaml`. | scope: service/user/payment.js, database/migrations/013_*, swagger/user.yaml | why: cut redundant DB round-trips per request and speed up coupon joins; response shape unchanged (no client contract change).
2026-06-14 — Added slug-based public bundle detail support via `GET /user/bundle/slug/:slug` while keeping the legacy numeric-id route unchanged. | scope: routes/user/bundle.js, controllers/user/bundle.js, service/managerial/bundle.js, docs/BOOKS_COURSE_BUNDLE_PAYMENT_FRONTEND_SPEC.md, .claude/FEATURES/course-bundle/purpose.md, .claude/memory.md | why: student combo pages now route by pretty slug (`bundle.url`), so the backend needed a first-class bundle-detail lookup that matches the frontend URL contract.
2026-06-14 — Removed the Assignment feature entirely (kept Quiz, which is MCQ auto-marked) + chapter/module field cleanup. Migration `014_remove_assignment_and_module_cleanup.sql` drops tables `assignment_files`/`submission`/`assignment` (children first), drops module cols `will_evaluated`/`assignment_question_doc_url`/`assignment_question_doc_type`, drops `chapter.phase`, drops `module.instructor_id` (+ FK `fk_module_instructor`), and adds Live-Class toggle cols `live_status`/`live_zoom_link`/`live_scheduled_at` to `module`. Code: removed assignment-document + module-instructor routes/controllers/services in `routes|controllers|service/managerial/moduleV2.js`; dropped instructor JOINs in `service/managerial/module.js` get/list and the ASSIGNMENT branch in completeModule; removed `phase` from `service/managerial/chapter.js` cols; cleaned ASSIGNMENT/instructor/will_evaluated/phase from `courseImportExport.js` import+export SQL and `courseV2.js` getFullEnhanced; removed the whole `getSubmissionAnalytics` endpoint (route+controller+service) and the `users_with_submissions` / course `submissions` engagement metrics in `analyticsV2Service.js` (+ metadata label); removed the `submission` queries from `service/managerial/user.js` (`activity` + history `submissions`); deleted orphaned `controllers/user/submission.js` (its service file was already missing) and the assignment/instructor swagger docs. Kept `quiz_time_limit`/`quiz_attempt_limit` and the unused `SUBMISSION` permission keys (RBAC infra, no broken refs). | scope: Math_Pro_backend (migration 014, moduleV2/module/chapter/courseV2/courseImportExport/user/analyticsV2 services, analytics route+controller, docs/managerial_module_v2, ASSIGNMENT_API_DOCUMENTATION.md) — RIPPLES to mathpro_admin (AssignmentModuleForm, submissions page, ModuleTypeSelector, instructor select, user-detail submissions, SubmissionAnalytics) and MathPro_Frontend (useAssignment + ASSIGNMENT module branches). Confirmed no production data in dropped tables. | why: simplify to a Bengali-first SSC/JSC/HSC LMS; assignment was the only human-graded/file-upload surface; Live Class becomes a toggle+status on a VIDEO module (recording filled in after class) instead of a module-type switch.

2026-06-14 — Phase 4 Live-Class toggle (separate mechanism from the existing `live` table feature, per user decision). Module gains a live overlay via flat columns `live_status` (SCHEDULED|LIVE|ENDED|null), `live_meeting_id`, `live_meeting_pass`, `live_scheduled_at` (epoch sec) — Zoom fields mirror the existing `live` table convention; the post-class recording reuses the module's normal VIDEO data (videoUrl/videoHost), so no recording column. Wired into `service/managerial/moduleV2.js` `updateEnhanced` (validates live_status enum; fields flow to frontend via `module.js` list/get `SELECT m.*` → course.getFull). Admin: live toggle added to `VideoModuleForm` (status/scheduled-at/meeting-id/passcode), sent on the v2 update-enhanced path; create path doesn't set live (author, then toggle on edit). Frontend: live banner in `ModulePlayer` shown when `live_status==='LIVE'` (Bengali copy, zoom id/pass/time); when ENDED it plays as a normal video. DEFERRED: pinning live modules to the public homepage — `landing-page.tsx` is currently a STATIC marketing mockup with no data fetch, so cross-course live-pinning is a separate feature needing its own endpoint + design. | scope: migration 014 (live_* cols), moduleV2.updateEnhanced; ripples to mathpro_admin VideoModuleForm + module.service/types, MathPro_Frontend ModulePlayer + module types | why: support "run a zoom live class as a module, then drop in the recording" without a module type-switch.

2026-06-14 — Removed the CODE module type entirely (online compiler + Codeforces), per the original TODO ask (was deferred from the assignment-removal plan, then explicitly requested). Migration 014 also drops tables `compilation` + `editorial_view` (both cascade from module) and explicitly drops `idx_module_chapter_will_evaluated` before its column. Code: removed user routes `viewEditorial`/`hasViewed`/`getLanguages`/`compile`/`getMyCompilations`/`quick-compile` (kept `addProgress`/`get`), their controller handlers, and the service methods `viewEditorial`/`hasViewed`/`getLanguages`/`quickCompile`/`getMyCompilations`/`compile` (Judge0/RapidAPI integration); simplified `completeModule(user_id,module_id,points)` (dropped the CODE editorial-gating branch + the now-dead `type` arg at the caller); removed orphaned `axios`/`millisForCompilerRetry`/`removeTrailingWhitespace` from module.js and `millisForCompilerRetry` from util/constants.js; dropped 'CODE' from courseImportExport validCategories; removed the getLanguages/compile/quick-compile/getCfHandle/checkCfStatus swagger paths from docs/managerial_module/index.js. NOTE: `COMPILER_API_KEY` env var is now unused (left .env alone). | scope: migration 014, routes/user/module.js, controllers/managerial/module.js, service/managerial/module.js, service/managerial/courseImportExport.js, util/constants.js, docs/managerial_module/index.js | why: simplify to a Bengali-first SSC/JSC/HSC math LMS — no in-browser code execution.
2026-06-14 — Renamed the backend npm package identity from `MathPro` to `mathpro` so local npm script banners and repo metadata match the product name. | scope: package.json, package-lock.json, AGENTS.md | why: remove stale Math Pro branding from backend runtime/config output.
2026-06-14 — Applied migration `014_remove_assignment_and_module_cleanup.sql` to the dev DB (dropped `assignment`/`submission`/`assignment_files`/`compilation`/`editorial_view`, dropped `chapter.phase`/`module.{instructor_id,will_evaluated,assignment_question_doc_*}`, added `module.{live_status,live_meeting_id,live_meeting_pass,live_scheduled_at}`) and end-to-end curl-tested with an admin token from `scripts/make-token.js`: chapter create/update/get (no phase), module update-enhanced with live_status SCHEDULED/LIVE/ENDED + invalid-enum rejection, user-facing module get shows live_* fields, addProgress/completeModule, module duplicate (12-col INSERT), analytics users/engagement + courses/:id/detailed (no submission refs), user get/history (no submissions field), quiz module create/export/update-enhanced. All passed. Also fixed a pre-existing unrelated bug found during testing: `exportCourseJSON()` in courseImportExport.js:835 read `const course = fullResult` but `getFullEnhanced()` returns `{success, data: {...}}`, so export always returned an empty course/chapters — changed to `fullResult.data`. | scope: dev DB migration applied (irreversible), courseImportExport.js:835 | why: verify the assignment/instructor/phase/live/CODE removal work end-to-end against a real DB before considering the task done.
2026-06-15 — Added `GET /user/module/live` (routes/user/module.js, controllers+service/managerial/module.js `getLiveModulesForUser`) returning all modules with `live_status IN ('SCHEDULED','LIVE')` across courses the authenticated user has access to (via `takes` UNION `bundle_course`/`bundle_purchase`), joined to `course` for `course_id`/`course_title`. Curl-tested with user token: returns `{success,data:[{id,title,data,is_live,live_status,live_meeting_id,live_meeting_pass,live_scheduled_at,course_id,course_title}],rowCount}`. Powers a new "live class now/upcoming" widget on the student `/dashboard` (MathPro_Frontend). | scope: routes/user/module.js, controllers/managerial/module.js, service/managerial/module.js (new method only) | why: student dashboard needs an aggregated cross-course live-class view; per-module live_status fields existed but had no "all my live classes" endpoint.
2026-06-15 — Hardened module progress writes for the LMS quiz flow: `/user/module/addProgress/:id` now passes the module `type`, `completeModule()` replaces prior non-quiz progress rows instead of accumulating duplicates, rejects a second `QUIZ` submission, and course score aggregation now sums the best point per module to avoid legacy duplicate-row inflation. | scope: controllers/managerial/module.js, service/managerial/{module,course}.js, .claude/memory.md | why: timed/weighted quizzes are final-answer only; repeated submits must not inflate a student's total course score.
2026-06-15 — Added `GET /user/module/live/:courseId` (routes/user/module.js, controllers+service/managerial/module.js `getLiveModulesForCourse`) — same enrollment-scoped query as `getLiveModulesForUser` but filtered to one `course_id`, used by the per-course dashboard's live-class list. Also added `promoteScheduledToLive()`: a global `UPDATE module SET live_status='LIVE' WHERE live_status='SCHEDULED' AND live_scheduled_at <= now()`, run at the top of both `getLiveModulesForUser`/`getLiveModulesForCourse` before the SELECT, so SCHEDULED→LIVE transitions happen automatically and persist on any GET to either endpoint. LIVE→ENDED remains manual (admin-set) — no duration field exists. | scope: routes/user/module.js, controllers/managerial/module.js, service/managerial/module.js | why: per-course "upcoming/live" list for MathPro_Frontend `/dashboard/[courseId]`; auto status flip removes the need to manually flip a module to LIVE at class start time.
2026-06-15 — Quiz attempts moved server-side (was localStorage-only). New `quiz_attempt` table (migration 015) keyed (user_id, module_id) stores answers jsonb + verdict jsonb + score; `progress` remains the one-attempt lock. New `service/managerial/quizAttempt.js` decrypts the encrypted answer key with `crypto-js` + `SECRET_KEY_QUIZ` (fail-safe → "" on bad data) and grades on the server, so the answer key never trusts the client. New `controllers/user/quizAttempt.js` + routes `POST /user/module/quiz/:id/submit` (grade+persist, rejects if already submitted) and `GET /user/module/quiz/:id/attempt` (restore submitted answers/verdict/score). Uses req.user.id (secure), unlike legacy addProgress which trusts req.body.user_id. Added `SECRET_KEY_QUIZ` to .env = the ADMIN encryptor key (frontend's NEXT_PUBLIC_SECRET_KEY_QUIZ differed and is likely stale). | scope: database/migrations/015_quiz_attempt.sql, service/managerial/quizAttempt.js, controllers/user/quizAttempt.js, routes/user/module.js, .env | clients: MathPro_Frontend (student quiz) — useQuiz now POST/GETs these; admin unaffected. | why: "perfect backend-based solution" for quiz submission detection + answer reveal, not localStorage.
2026-06-15 — Rebranded backend copy from CoderVai/Binary School to Math Pro across README, app health text, emails, docs, and seeded examples; kept URLs valid by switching hostnames/emails to `mathpro.*`. | scope: backend copy/docs/templates/messages | why: remove legacy brand references and align the backend with Math Pro naming.
2026-06-15 — Fixed course dashboard "no access" for combo/bundle-enrolled students. `getDashboard` (service/managerial/course.js, Query 4 enrollment check) only queried `takes` (direct purchases), so a user enrolled via a Combo (bundle_purchase → bundle_course) got "You do not have access to this course." Added a UNION ALL against `bundle_purchase bp JOIN bundle_course bc` so bundle enrollment also counts (same dual-source pattern getLiveModulesForUser already uses). Verified live: user 1 (takes 8,9 + bundle 2 → courses 2,7) now passes dashboard/2 & /7; non-enrolled course 1 still correctly denied. NOTE the JWT secret was a red herring — token verified fine on prod; bug was purely the takes-only enrollment gate. | scope: service/managerial/course.js | clients: MathPro_Frontend /dashboard/[courseId]. | why: combo students were locked out of their own course dashboards.
2026-06-15 — Split the course-dashboard auth/enrollment failures into distinct messages (were all collapsing into one generic "Authentication required / please enroll", which masked token problems as enrollment problems). `optAuthenticateUser` (service/authMiddleWares.js) now sets `req.body.auth_error` = 'no_token' | 'expired' (TokenExpiredError) | 'invalid' alongside the existing `auth=false`; its `auth` contract is unchanged so the other optional-auth routes (getFull, getScore, prebook, routine, announcements) are unaffected. `getDashboard` (service/managerial/course.js) maps these to "Please log in…", "Your session has expired. Please log in again.", "Your session is invalid. Please log in again.", and keeps the separate not-enrolled message. Verified live: 6 cases (no token / invalid / expired / valid-not-enrolled / valid-combo / valid-direct) each return the right message. | scope: service/authMiddleWares.js (additive auth_error only), service/managerial/course.js | clients: MathPro_Frontend dashboard — can now distinguish expired→silent re-login from not-enrolled. | why: expired/wrong-secret tokens looked identical to "not enrolled", which sent debugging down the wrong path.
2026-06-15 — Expanded the student/profile JSON contract with fixed keys (facebookId, address, schoolCollege, group, guardianName, guardianMobile, relationWithGuardian, gender, classLevel, version) and preserved legacy aliases `currentInstitution`/`currentAcademicLevel` for compatibility. | scope: controllers/user/profile.js, service/user/profile.js, docs/index.js, docs/managerial_auth/{get_profile,set_profile,PROFILE_API_DOCUMENTATION}.js, docs/components.js | why: store structured personal-information fields in `managerial_auth.profile` without breaking existing checkout/profile consumers.
2026-06-15 — Dropped the `currentInstitution`/`currentAcademicLevel` compatibility aliases from the student/profile contract after user confirmation; the checkout profile path now reads and returns only the fixed keys plus `department` and auth metadata. | scope: controllers/user/profile.js, service/user/profile.js, docs/index.js | why: keep the contract minimal and avoid carrying legacy aliases forward.
2026-06-16 — Hardened the public coupon surface consumed by MathPro_Frontend: stripped coupon `code` from public course/bundle detail payloads and `GET /user/coupon/course/:course_id`, changed `POST /user/coupon/apply` to price off the authoritative DB course/bundle price instead of trusting client `original_price`, and ignored unauthenticated `user_id` values on the optional-auth validate/apply routes. | scope: controllers/user/coupon.js, service/managerial/{coupon,course,bundle}.js, docs/user_coupon/* | why: coupon codes were being exposed in public network responses and discount calculations were partially client-influenced.
2026-06-16 — Simplified coupon internals without changing the public route shape: unified course/bundle coupon lookup, applicability, user-eligibility, active-coupon listing, and public preview formatting behind shared helpers in `service/managerial/coupon.js`; updated the legacy course `applyCoupon` path to reuse the canonical coupon-application flow instead of re-validating and re-pricing separately. | scope: service/managerial/{coupon,course,bundle}.js | why: reduce duplicated coupon logic and lower the risk of course/bundle behavior drifting on future fixes.
2026-06-16 — Removed the aftermessage backend feature and table: dropped `aftermessage` from the base schema, added migration `017_drop_aftermessage.sql`, removed the admin/user routes/controllers/service and OpenAPI docs, and cleaned the stale schema snapshots. | scope: app.js, database/migrations/000_base_schema.sql, database/migrations/017_drop_aftermessage.sql, routes|controllers|service/*/aftermessage.*, docs/aftermessage*, docs/index.js, docs/components.js, old_schema*.sql | why: remove the table and every backend reference to it.
2026-06-16 — Removed the legacy `users` and `user_tags` tables from the backend schema, added migration `018_drop_users_and_user_tags.sql`, and applied it to the configured database. | scope: database/migrations/000_base_schema.sql, database/migrations/018_drop_users_and_user_tags.sql, old_schema*.sql | why: these tables were unused by the live backend, which reads user data from `managerial_auth` instead.
2026-06-16 — Removed the remaining legacy feature tables and backend references: dropped `certificate`, `contest_participants`, `contact_submissions`, `contest`, `gift`, `homepage_data`, `interest`, `issue`, `level`, `live`, `live_feed`, `response`, and `system_config`; deleted the associated admin/user routes, controllers, services, and docs; trimmed RBAC/course-access helpers; and applied migration `019_drop_removed_feature_tables.sql`. | scope: database/migrations/000_base_schema.sql, database/migrations/019_drop_removed_feature_tables.sql, util/{permissions,courseAccessHelpers}.js, app.js, controllers|routes|service deleted files, docs deleted files, old_schema*.sql | why: remove unused LMS surfaces and keep the backend schema/docs aligned with the live API contract.
2026-06-16 — Dropped the unused `device_tokens` table and applied migration `022_drop_device_tokens.sql`; the backend has no live read/write path for device token storage, so keeping the table only preserved stale FCM scaffolding. | scope: database/migrations/000_base_schema.sql, database/migrations/022_drop_device_tokens.sql, old_schema*.sql | why: remove dead push-notification storage that was not used by the current backend code.
2026-06-16 — Removed the legacy `activity` table and `/user/activity` endpoint: deleted the user activity controller/service/route and their docs, stripped the schema snapshots, and applied migration `023_drop_activity.sql`. | scope: app.js, database/migrations/000_base_schema.sql, database/migrations/023_drop_activity.sql, controllers/user/activity.js, routes/user/activity.js, service/user/activity.js, docs/managerial_auth/activity.js, old_schema*.sql | why: the endpoint had no frontend/admin consumers and only duplicated raw page-visit logging that is not used by streak tracking.
2026-06-16 — Fixed a critical payment-pricing vulnerability: `initiatePayment` and `initiatePaymentForBundle` no longer accept a client-supplied price via `eventId` (was `originalPrice = body.eventId / 6251`); price is now always read from the course/bundle DB row. Also made the IPN amount check authoritative — the SSLCommerz validated amount is now enforced even when the IPN body omits `value_c` (previously the check was gated on a truthy client amount). | scope: service/user/payment.js, controllers/user/payment.js | why: clients could set any price (e.g. buy a course for 1 BDT) because SSLCommerz only validates paid==requested; this closes that hole. Cross-repo: MathPro_Frontend stopped sending `eventId`. NOTE for follow-up: unauthenticated dead endpoints initiatePaymentTmpBkash/Nagad still accept arbitrary client total_amount with hardcoded tran_id REF123 — flagged, not removed.
2026-06-16 — Removed the dead, unauthenticated `initiate-tmp-bkash` / `initiate-tmp-nagad` payment endpoints (route + controller methods + service methods). They had no client callers in Frontend or Admin and accepted an arbitrary client `total_amount` with a hardcoded `tran_id: "REF123"` — an arbitrary-price/unauth hole. Verified: removed routes now 404, `/user/payment/initiate/:id` still 401s (intact). | scope: routes/user/payment.js, controllers/user/payment.js, service/user/payment.js | why: eliminate unused insecure surface following the eventId pricing fix; no client consumes these.
2026-06-17 — Swapped the backend presigned upload helper from AWS-only config to R2-aware storage config: it now supports R2 endpoint/account/public URL base envs, still returns the same presigned-upload envelope, and keeps AWS-compatible fallback behavior for legacy environments. Updated the admin/user upload route docs to say generic storage upload instead of S3. | scope: util/presignedUploadService.js, docs/managerial_upload_v2/upload_presigned_url.js, docs/user_upload_v2/upload_presigned_url.js | why: move file storage off AWS S3 while preserving the existing API contract for both clients. Clients affected: MathPro_Frontend + mathpro_admin.
2026-06-17 — Removed the temporary AWS-compatible fallback from the upload helper so R2 is now the only supported storage backend; the helper now reads only `R2_*` envs and the local backend env comment was renamed from S3 to R2. | scope: util/presignedUploadService.js, .env | why: the project will not use AWS storage anymore, so the backend contract should not retain legacy fallback logic.
2026-06-17 — Added a backend `.env.example` for the new R2 storage contract and renamed the live backend env keys from `AWS_*` to `R2_*` (`R2_ACCOUNT_ID`, `R2_BUCKET`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_PUBLIC_URL_BASE`). | scope: .env, .env.example | why: make the storage config explicit and match the R2-only upload helper.
2026-06-17 — Added a dedicated `quiz-image` upload purpose mapped to `quizzes/images/` and updated the admin upload docs example accordingly. | scope: util/uploadPolicies.js, docs/managerial_upload_v2/upload_presigned_url.js | why: rich-text uploads from quiz question editors should be separated from announcement attachments while using the same presigned upload endpoint.
2026-06-17 — Added admin storage delete support for uploaded objects via `POST /v2/admin/upload/delete`, with backend resolution from either object key or public URL. | scope: util/presignedUploadService.js, controllers/managerial/uploadV2.js, routes/managerial/uploadV2.js, docs/managerial_upload_v2/** | why: let the admin course thumbnail flow delete the previous object on clear/replace while keeping the upload contract additive. Clients affected: mathpro_admin.
2026-06-17 — Added a dedicated `course-outline` upload purpose mapped to `courses/outlines/` so the admin course outline field can upload PDFs instead of only accepting links. | scope: util/uploadPolicies.js | why: support the new admin course-outline PDF option without reusing an unrelated upload purpose.
2026-06-17 — Stopped public course detail responses from exposing `active_coupons`; `/user/course/getfull/:id` and the slug variant now return coupon previews only for authenticated requests and an empty array for anonymous visitors. | scope: service/managerial/course.js | why: keep coupon promos off the public course-details payload while preserving the rest of the response shape.
2026-06-18 — Disabled email delivery in announcement sends so the backend only honors `notification` as a delivery method, and awaited the notification insert/status update before returning from `/admin/announcement/send/:id`. | scope: service/managerial/announcement.js | why: web notifications are the only supported announcement delivery path for now.
2026-06-18 — Linked course notifications back to announcements with `notification.announcement_id`, normalized announcement notifications from the joined announcement record, and kept rich HTML in the announcement source of truth. | scope: database/migrations/024_link_notifications_to_announcements.sql, service/managerial/announcement.js, service/user/notification.js, controllers/user/notification.js, docs/components.js | why: announcement content and notification delivery/state now share one backend contract for course broadcasts.
2026-06-18 — Hard-wired announcement create/update to student-only + web-notification delivery on the API side, ignoring any client-supplied recipient/channel values. | scope: service/managerial/announcement.js | why: admin should not choose send-to or channel anymore; backend is the enforcement point.
2026-06-18 — Trimmed the admin announcement docs to match the enforced student/web delivery contract and removed the obsolete send-channel request body. | scope: docs/index.js | why: the route docs were still advertising channel toggles that the API now ignores.

2026-06-18 — Auth follow-up: (1) OTP is now PHONE-ONLY (SMS) — requestOTP/register/forgot/reset-password reject email identifiers (cost control); login still accepts email as an identifier. (2) Email is verified ONLY via Google: new /admin/auth/connect-google (authenticated) attaches OR replaces the Google-verified email on a logged-in (typically phone) account, with a uniqueness guard (email/login not owned by another user); re-running with a different Google account changes the email. No email-OTP, no /link-email. (3) login() now matches the CANONICAL column only — email identifiers via the `email` column, phone via `phone` — dropped the `OR login=$1` fallback. No data migration needed: all type=3 rows already have email in the email column (login==email duplicate). | scope: service/managerial/auth.js (requestOTP, register, resetPasswordWithOTP, login, connectGoogle), controllers/managerial/auth.js, routes/managerial/auth.js | why: reduce SMS/email cost (phone OTP only), make email a Google-verified-only field, and make the email column the single login key. Clients: FRONTEND (FE work pending: phone signup+OTP, single-field login, Connect-Google button, SESSION_REVOKED).

2026-06-18 — Phone-based auth for regular students (type=3) on /admin/auth/*. (1) requestOTP/verifyOTP now accept phone OR email (detectContactType), with per-contact resend cooldown (60s), daily cap (5), single-active-OTP, and an attempts cap (5 via new otp.attempts col). (2) register is phone-primary and now REQUIRES+verifies OTP before creating the account; login accepts phone or email (login OR phone/email column). (3) Google login matches by verified email and links via profile.auth_providers; new linkPhone endpoint lets Google-first users add phone+password+OTP. (4) Device limit: new auth_session table + `sid` JWT claim; createSession evicts oldest beyond 2; authenticateUser validates sid for type=3 (legacy sid-less tokens pass through); reset-password revokes all sessions; added /logout, /sessions, /verify-otp, /link-phone routes. (5) Removed dead /in/auth path (routes/controllers/service/mount/swagger) + dropped in_auth table. | scope: migrations 025_auth_session/026_managerial_auth_phone_unique/027_otp_attempts/028_drop_in_auth, service/managerial/auth.js, controllers/managerial/auth.js, routes/managerial/auth.js, service/authMiddleWares.js, util/authHelpers.js, app.js, docs/index.js, docs/PHONE_AUTH_REDESIGN_PLAN.md | why: move student identity to phone+OTP, support Google↔phone linking, enforce max-2-devices. Contract change consumed by FRONTEND (/admin/auth/*) — FE work (phone field, OTP step, SESSION_REVOKED handling) pending; admin login (type 1/2, email) unchanged and session-less.
2026-06-18 — Added a backend rate-limiting guide documenting the current phase-1 in-memory policy, shared limiter helpers, and the enforced hotspot matrix across auth, payments, uploads, coupon abuse, public reads, feedback, module activity, streaks, and admin analytics. | scope: docs/RATE_LIMITING_GUIDE.md, .claude/memory.md | why: keep the throttling policy explicit for future route work and avoid ad hoc limiter placement.

2026-06-18 — Central-auth fix: /admin/auth/google now allows admins/moderators (type 1/2), not just students. Removed the `if (user.type !== regular) return 'not allowed for student login'` guard in googleLogin — under centralized auth the Frontend login (incl. Google) authenticates admins too, and the type-based redirect sends them to the admin app. Token `type` still comes from the DB (Google only proves email ownership, no privilege escalation). Also made the Google-path session creation conditional on type=3 (admins stay session-less, matching password login + the type=3-only middleware session check). | scope: service/managerial/auth.js (googleLogin) | why: promoting an admin (type=1) made Google login fail with 'not allowed for student login'; central auth requires admins to log in via the shared Frontend Google flow. Contract change consumed by FRONTEND (no client code change needed; admin redirect already type-based).
2026-06-19 — Changed the user notification count endpoint to count unread rows (`is_read = false`) instead of bell-click state so the student navbar can show a real unread badge. | scope: service/user/notification.js | why: badge count must decrease when a student reads notifications, not only when the bell is clicked.
2026-06-19 — Added derived combo tags to bundle payloads and included child course tags in bundle list/detail/history responses so the frontend can match combos to course-directory/category filters. | scope: service/managerial/bundle.js | why: combo cards now inherit child-course tags for consistent filtering and promotion on the student frontend.
2026-06-19 — Added child-course `x_price` to bundle payloads so the frontend combo card can render the same crossed-out original-price pattern as regular course cards using the summed underlying course price. | scope: service/managerial/bundle.js | why: combo cards now need a real original-price base, not a separate savings/count block.
2026-06-29 — Perf Stage 3 (backend half): added on-demand Frontend cache revalidation. NEW util/revalidateFrontend.js — fire-and-forget axios POST to ${FRONTEND_URL}/api/revalidate with header x-revalidate-secret===REVALIDATE_SECRET, body {tags:[...]}; never throws (failed/unreachable frontend must not affect admin request). Exports revalidateOnWrite(tags) Express middleware that fires on res 'finish' for 2xx POST/PUT/PATCH/DELETE only. Mounted via router.use() on the admin mutation routers: courseV2 + deprecated course -> ['courses','combos'] (a course write can change combos containing it); bundle -> ['combos']; teacherV2 + deprecated teacher -> ['instructors','courses'] (teacher write can change a course's instructors); testimonial -> ['public-testimonials']. Reused existing FRONTEND_URL env; needs NEW env REVALIDATE_SECRET (not added to .env — user to set, must match frontend). Used axios not global fetch to respect engines node>=16. Clients affected: Frontend (must add /api/revalidate route + REVALIDATE_SECRET + tag the catalog fetches — NOT yet built). npm run lint passes. | scope: util/revalidateFrontend.js (new), routes/managerial/{courseV2,course,bundle,teacherV2,teacher,testimonial}.js | why: refresh student public pages within seconds of an admin publish instead of waiting on the ISR timer.
